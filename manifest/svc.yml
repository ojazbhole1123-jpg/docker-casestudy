apiVersion: v1
kind: Service
metadata:
  name: docker-case-study
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: docker-case-study
  ports:
    - port: 3200
      targetPort: 3200
      protocol: TCP
